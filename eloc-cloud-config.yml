################################
#
# Actuator 配置
#
################################

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
    
  
